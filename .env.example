# ===== Supabase =====
# Supabase 프로젝트 URL - 브라우저에서 접근 가능 (공개 값)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase Anon Key - 브라우저에서 접근 가능 (공개 값, RLS로 보호됨)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase Service Role Key - 서버에서만 사용 (절대 NEXT_PUBLIC_ 으로 설정 금지)
# RLS를 우회하여 DB에 직접 접근 - 결제 상태 업데이트, 무료 슬롯 관리 등에 사용
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ===== Anthropic (AI) =====
# Claude API Key - 서버에서만 사용 (절대 NEXT_PUBLIC_ 으로 설정 금지)
ANTHROPIC_API_KEY=sk-ant-your-key

# ===== PortOne (결제) =====
# PortOne V2 Store ID - 브라우저에서 접근 가능
NEXT_PUBLIC_PORTONE_STORE_ID=your-store-id

# PortOne V2 Channel Key - 브라우저에서 접근 가능
NEXT_PUBLIC_PORTONE_CHANNEL_KEY=your-channel-key

# PortOne V2 API Secret - 서버에서만 사용 (결제 검증용)
PORTONE_API_SECRET=your-api-secret

# ===== 카카오 OAuth =====
# Supabase Auth에서 관리하므로 별도 설정 불필요
# Supabase 대시보드 > Authentication > Providers > Kakao 에서 설정
